name: test
channels:
  - conda-forge
  - defaults

dependencies:

  # psi4
  - psi4::dftd3
  - psi4::psi4 >= 1.3.0

  # other qm
  - geometric

  # - h5py<3.2

  # docs
  - ipython
  - jupyter
  - numpy
  # - openff-toolkit-base
  - pip
  # - postgresql >= 13.0
  - qcelemental==0.22.0
  - qcengine
  - qcfractal
  - qcfractal-core
  - rdkit
  - requests
  - scipy
  # Pip-only installs
  - pip:
      - pytest
      - pytest-cov
      - pytest-xdist
      - autodoc_pydantic
      - duecredit
      - importlib_resources
      - mdanalysis>=2.0
      - myst-parser
      - sphinx>2.0
      - sphinx_rtd_theme
      - sphinxcontrib-bibtex<2.0.0
      - sphinx-sitemap
