name: test
channels:
  - conda-forge
  - psi4
dependencies:
  # Base depends
  - python
  - pip
    # Testing
  - pytest
  - pytest-cov
  - codecov

  # program
  - numpy
  - psi4=1.3.2
  - qcelemental

  # Pip-only installs
  - pip:
      - duecredit
