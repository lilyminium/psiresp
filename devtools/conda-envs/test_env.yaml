name: test
channels:
  - conda-forge
  - psi4
dependencies:
  # Base depends
  - python
  - pip
    # Testing
  - pytest
  - pytest-cov
  - codecov
  - typing_extensions

  # program
  - cython
  - numpy>=1.20.0
  - psi4>=1.3.2
  - qcelemental
  - rdkit

  # Pip-only installs
  - pip:
      - duecredit
      - pytest-xdist
      - mdanalysis==2.0.0b0
      - pyyaml
