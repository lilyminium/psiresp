name: test
channels:
  - conda-forge
  - psi4
dependencies:
  # Base depends
  - python
  - pip
    # Testing
  - pytest
  - pytest-cov
  - codecov

  # program
  - numpy
  - psi4=1.3.2
  - qcelemental
  - rdkit

  # Pip-only installs
  - pip:
      - duecredit
      - pytest-xdist
      - git+https://github.com/MDAnalysis/mdanalysis@develop#egg=mdanalysis&subdirectory=package