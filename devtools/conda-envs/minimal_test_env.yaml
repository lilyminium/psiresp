name: test
channels:
  - conda-forge
  - defaults

dependencies:

  # core
  - python
  - pip

  # qcstack
  - qcelemental

  # deps
  - numpy
  - scipy >= 1.4
  - tqdm

  # Pip-only installs
  - pip:
      - pytest
      - pytest-cov
      - pytest-xdist
      - importlib_resources
      - msgpack-python
