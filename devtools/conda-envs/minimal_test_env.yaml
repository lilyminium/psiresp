name: test
channels:
  - conda-forge
  - defaults

dependencies:

  # core
  - python
  - pip

  # qcstack
  - qcelemental == 0.22.0

  # deps
  - numpy
  - scipy >= 1.4
  - tqdm

  # testing
  - pytest >= 4.6.4
  - pytest-cov
  - pytest-xdist


  # Pip-only installs
  - pip:
      - importlib_resources
      - msgpack-python
