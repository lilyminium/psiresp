name: psiresp-docs
channels:
  - psi4
  # - conda-forge

dependencies:
  - pip
  - psi4
  - python
  # Pip-only installs
  - pip:
      - duecredit
      - numpy>=1.20.0
      - qcelemental
      - sphinx
      - sphinx_rtd_theme
      - sphinxcontrib-bibtex<2.0.0
      - sphinx-sitemap