name: test
channels:
  - conda-forge
  - psi4

dependencies:
  # Base depends
  - pip
  # Testing
  - pytest
  - pytest-cov
  - codecov

  # # program
  - cython
  - mdanalysis
  - numpy
  - psi4
  - qcelemental
  - rdkit
  - scipy
  # # Pip-only installs
  - pip:
      - duecredit
      - pytest-xdist
      - pyyaml
      - typing_extensions
